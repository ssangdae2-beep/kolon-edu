// 실습 07(10장)에서 Bob과 함께 완성할 검증 유틸입니다.
// 현재는 아무 검증도 하지 않는 자리표시자 상태입니다.

function isValidTitle(title) {
  // TODO: 빈 문자열, 공백만 있는 문자열, undefined를 모두 걸러내야 합니다.
  return true;
}

module.exports = { isValidTitle };
